import { FormatPlayCountPipe } from './format-play-count-pipe';

describe('FormatPlayCountPipe', () => {
  it('create an instance', () => {
    const pipe = new FormatPlayCountPipe();
    expect(pipe).toBeTruthy();
  });
});
